const express = require("express");
const {
 
} = require("../controller/");

const router = express.Router();

// Notification routes
router.get("/", );
router.post("/", );
router.put("/", );
router.delete("/", );

module.exports = router;
